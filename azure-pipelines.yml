# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Default
  vmImage: ubuntu-latest

stages:
  - stage: teststage
    displayName: 'test-stage to see script execution'
    jobs:
      - job: validate
        workspace:
          clean: all
        steps:
        - script: |
            ls



